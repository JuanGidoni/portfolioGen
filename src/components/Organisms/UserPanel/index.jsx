const UserPanel = ({user}) => {
 return (
  <div>
   Bienvenido a mi portfolio, {user.displayName}. <br/>
   Por el momento, no hay nada para hacer siendo un usuario visita. <br/>
   Vuelve pronto para más.
  </div>
 )
}

export default UserPanel
